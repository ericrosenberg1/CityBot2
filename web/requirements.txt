# Web dashboard dependencies — these are now included in the root requirements.txt.
# Install from the project root: pip install -r requirements.txt
fastapi>=0.136.1
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
